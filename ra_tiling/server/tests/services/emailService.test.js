import { describe, it, expect, vi, beforeEach } from 'vitest';


// should correctly parse and sanitise the form input

describe('email')


// should correctly rate limit


// should correctly send to email